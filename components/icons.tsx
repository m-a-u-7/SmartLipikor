

import React from 'react';

// Logo component removed as it is no longer used.

// You can add more icons here if needed, or re-export from lucide-react
export { UploadCloud, Copy, Download, FileText, FileImage, FileType, AlertTriangle, XCircle, RefreshCw } from 'lucide-react';
